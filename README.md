# pvisc-football-image-analysis

Instructions:

1. run <python -m venv env>
2. run <.\env\Scripts\Activate>
3. run <pip install -r requirements.txt>
4. rename <.env_example> to <.env> and write your roboflow's API key